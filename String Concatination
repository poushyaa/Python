#String concatination
#if String-> AAABBCCCDDD,
#Required output is -> A3B2C3D3
a="AAACCCBAABB"
count=1  #if we keep count=0 then while it is comparing two values,the count will be incremented to 1,but it should be 2 
#because two values we are comparing and 2 should be the count ,in order to print the count of characters in string

res="" #for storing the result we have taken an empty string
for i in  range(len(a)):
    if i+1<len(a) and a[i]==a[i+1]:
        count=count+1
    else:
        res=res+a[i]
        res=res+str(count)

        count = 1  #after printing the count of occurence of same character,the count is set to 1
print(res)
